#include "main.h"

/**
 * mul -  a function that multiplies two digits together
 * @c: input
 * Return: the result of the function
 */
int mul(int a, int b)
{
	int result;
	result = a * b;
	return (result);
}
